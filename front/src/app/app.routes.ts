import { Routes } from '@angular/router';
import { ScoreCalculator } from './components/score-calculator/score-calculator';


export const routes: Routes = [
    { path: '', component: ScoreCalculator }
];
